# konkrete Beispiele zur Diskussion

## Hashtag aus Ticket 878

* `${.if:${.gt:.timeYear:2006}:#Area 51 #SolarEnergy:}`
* `${.gt:.timeYear:2006:#Area 51 #SolarEnerg}`
* `${.gt:.timeYear:2006:${hashTag}}`

## Wie sehen die bisherigen globalen Werte aus

* `%WORLDTIME:GERMANCURRENCY%` - kann entfallen; wo die Unterscheidung wichtig ist, kann sie mit der neuen Syntax direkt implementiert werden

# TODO (echte) Anwendungsbeispiele, die mit den Vorschlägen Probleme bereiten könnten